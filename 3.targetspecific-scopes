.PHONY: all foo bar baz

VAR = global scope

all: foo bar
all: ; @echo In $@ VAR is $(VAR)

foo: ; @echo In $@ VAR is $(VAR)

bar: VAR = local scope
bar: baz
bar: ; @echo In $@ VAR is $(VAR)

baz: ; @echo In $@ VAR is $(VAR)


 
#intenta hacer all, como depende de foo y bar los intenta hacer primero
#foo aparece primero entonces intenta hacer foo
#como foo no depende de nadie, hace foo 
#luego intenta hacer bar
#bar depende de baz por lo que hace baz primero
#luego hace bar
#luego como cumplio todas las dependencias de all
#hace all

#Este es el standard que sigue make, "depth-first, left-to-right search pattern"

